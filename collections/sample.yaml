- name: Get namespace name
  hosts: localhost
  collections:
    - oracle.oci
  tasks:
    - pip:
      name: oci
    - name: Get namespace name
      oci_object_storage_namespace_facts:
      register: output
    - name: Print namespace name
      debug:
        msg: "{{ output }}"
